language: python
python:
  - "2.7"
install:
  - "pip install coverage"
  - "pip install coveralls"
script:
  - "cd test && ./test.sh"
after_success:
  - "cd test && coveralls"
sudo: false
notifications:
  email: false
